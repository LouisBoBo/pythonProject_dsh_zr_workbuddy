# 每日复盘 · 2026-09-09

> 范围：删除菜单车道体验收口；结论与过程对齐；prepare/Symbol 根治；删除路径安全门禁；文档知识库分类  
> 仓库：DSH-ZR-WorkBuddy（`apps/zr-workbuddy` + `scripts/` + `docs/`）  
> 约束：单 bridge + `features/`；引擎回环；HITL nonce；业务验收认引擎

---

## 1. 今日目标与结果

| 目标 | 结果 |
|------|------|
| 删除过程=Cursor 对话、结论不卡顿 | **P** |
| 结论「改动文件」与过程清单一致（动态提取，非写死功能） | **P** |
| 「本轮运行失败 reading prepare」彻底修掉 | **P**（profile dsh-tools realpath） |
| 写码结论对齐删除车道 | **暂缓**（产品确认后补） |
| 安全自查并修 | **P**（穿越门禁、收窄 write_scope、共用壳拒绝） |
| 文档分类维护 | **P**（`docs/README` 分册 + 专篇） |

---

## 2. 今日做了什么

- 删除：过程清单解析 → 校验 → 引擎删/补菜单路由/`main.py` → 结论按过程顺序对齐落盘。  
- 宿主：`check-vendor.sh` 覆盖 profile；install/restart 后自动对齐；bridge 启动断言 prepare。  
- 安全：路径穿越、禁止整棵 `backend/app/`、扩大共用壳 deny、gitignore 应用侧 `*.env`。  
- 文档：知识库七类索引；删除专篇；运维排障 prepare；安全附录；今日复盘。

---

## 3. 踩坑表

| 坑 | 原因 | 处理 |
|----|------|------|
| 结论缺 API/后端 | 过程路径被拒（write_scope/白名单） | 动态放行专属 api/routers + 收窄共用壳 |
| 只拦 run_code 仍 prepare | 崩在 agent-loop Symbol，guard 太晚 | 对齐 profile↔宿主 dsh-tools |
| 写死功能映射难维护 | 产品要求灵活 | 从过程删除清单提取 |

---

## 4. AGENTS 自检

- [x] 能力仍在 `features/code-dev` + `engine/app/code_dev`  
- [x] 未增第二 bridge；未改 host 内核焊业务  
- [x] 密钥未进文档/feature  

---

## 5. 产物

- `docs/README.md`（分类知识库）  
- `docs/功能实现/P0-1-删除菜单车道.md`  
- `docs/宿主与运维排障/*`  
- `scripts/check-vendor.sh` 扩展；相关引擎安全补丁  

---

## 6. 明日关注

- 写码结论按同步真实文件对齐（产品再开）  
- 市场插件硬依赖 dsh-tools → peerDependency（供应链侧）  

---

## 7. 一句话结论

删除车道可交付；prepare 根因是 dsh-tools 双实例；知识库已按产品/架构/安全/功能/运维/复盘分类，方便后续维护。
